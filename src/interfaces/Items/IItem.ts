export interface IItem {
  description: string;
  itemStatus?: boolean;
  userAvatar: string;
  setRefresh?: any;
  token: string;
  username: string;
  title: string;
  userId: any;
  itemId: number;
  type: string;
  userRole?: string;
  refreshPage?: boolean;
}
